NEXT_PUBLIC_API_URL=http://localhost:4000

# Optional: set to enable real Facebook OAuth login.
# If NOT set, the app uses DEV_AUTH_BYPASS flow.
# NEXT_PUBLIC_FACEBOOK_APP_ID=

# Optional (development only): when NEXT_PUBLIC_FACEBOOK_APP_ID is NOT set,
# the login page requires this Facebook user id for DEV_AUTH_BYPASS.
# NEXT_PUBLIC_DEV_FACEBOOK_ID=
