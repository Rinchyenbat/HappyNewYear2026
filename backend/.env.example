# Server
PORT=4000
NODE_ENV=development

# Mongo
MONGODB_URI=mongodb://127.0.0.1:27017/hn2026

# JWT
JWT_SECRET=change_me_in_production
JWT_EXPIRES_IN=7d

# Clerk JWT verification (used by POST /auth/clerk/exchange)
# Provide the JWKS URL and issuer from your Clerk instance.
# Example:
# CLERK_JWKS_URL=https://<your-clerk-domain>/.well-known/jwks.json
# CLERK_ISSUER=https://<your-clerk-domain>
CLERK_JWKS_URL=
CLERK_ISSUER=

# Optional: enables fetching external_accounts from Clerk API.
# If set, we will try to store the real Facebook provider user id in User.facebookId.
CLERK_SECRET_KEY=

# Optional: bootstrap the first admin (recommended in production)
# If set, logging in with this Facebook user id will create an admin user once.
ADMIN_FACEBOOK_ID=
ADMIN_USERNAME=admin

# CORS
CORS_ORIGIN=http://localhost:3000

# Frontend
# (Used only for CORS / local development convenience)
# FRONTEND_URL is no longer used for auth redirects.
